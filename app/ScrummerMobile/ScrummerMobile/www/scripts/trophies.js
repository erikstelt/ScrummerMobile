// Scripts for the trophies page of the application
(function () {
    "use strict";

    // When the document is loaded we set up the flickity slider
    document.addEventListener('DOMContentLoaded', function () {
        // Initialize main slider
        alert("works");
    });
});